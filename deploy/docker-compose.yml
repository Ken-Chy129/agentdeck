services:
  agentdeck:
    build: ..
    image: agentdeck:latest
    container_name: agentdeck
    restart: unless-stopped
    ports:
      - "127.0.0.1:8480:8480"
    volumes:
      - /opt/agentdeck/data:/data
