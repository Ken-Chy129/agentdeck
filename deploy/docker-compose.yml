services:
  skillhub:
    build: ..
    image: skillhub:latest
    container_name: skillhub
    restart: unless-stopped
    ports:
      - "127.0.0.1:8480:8480"
    volumes:
      - /opt/skillhub/data:/data
